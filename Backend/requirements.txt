# Core Django (LTS - Python 3.14 compatible)
Django==5.2.8

# REST Framework
djangorestframework==3.16.1
djangorestframework-simplejwt==5.3.1

# API Documentation
drf-spectacular==0.27.0

# CORS Headers
django-cors-headers==4.3.1

# Filtering
django-filter==23.5

# Admin Theme
django-jazzmin==3.0.1

# Image Processing (compatible with Python 3.13)
Pillow>=10.4.0

# PostgreSQL Database Adapter (commented out - using SQLite for now)
# psycopg2-binary==2.9.9

# Environment Variables (we're using os.environ now, so this is optional)
# python-decouple==3.8

# TODO: Encryption for Sensitive Data
# Note: Field-level encryption will be implemented at application layer using Fernet
# due to Windows compatibility issues with django-cryptography
